{
  "main": "dist",
  "private": true,
  "scripts": {
    "test": "npm run build && npm run lint",
    "lint": "eslint --ext .js,.jsx,.ts,.tsx src",
    "build": "tsc -p .",
    "watch": "tsc -p . -w --preserveWatchOutput --inlineSourceMap",
    "dev": "tabris serve -a -w"
  },
  "dependencies": {
    "tabris": "~3.8.0"
  },
  "devDependencies": {
    "eslint": "^8.0.1",
    "typescript": "~3.8.3"
  }
}
